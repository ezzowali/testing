<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ezzo.testing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testing"
        android:usesCleartextTraffic="true">
        <activity android:name=".upload_image"></activity>
        <activity android:name=".show_Qus_stu" />
        <activity android:name=".show_subjectStudent" />
        <activity android:name=".write_Qus" />
        <activity android:name=".add_subjectAdmin" />
        <activity android:name=".show_subjectAdmin" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>